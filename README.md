
Seed project for React apps using ES6, Webpack and HMR.

## How to install

* Run `npm install`
* Run `npm start`
* Open `http://localhost:3000` in your browser

## Development use

* Run `npm start`

## Production use

* Run `npm run build`